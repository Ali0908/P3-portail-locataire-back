spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/PORTAIL_LOCATION
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect

application:
  security:
    jwt:
      secret-key: 7dd70510db59db38b39dfe31a06422d0baccb877407ef8ff4c24065e23fd7517
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days
server:
  port: 3001
  cors:
    allowed-origins: "http://localhost:3001"
    allowed-methods: "GET,POST,PUT,DELETE"
    allowed-headers: "Content-Type,Accept,Authorization"


